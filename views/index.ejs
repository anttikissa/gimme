<!doctype html>
<meta charset=utf-8>
<title>Hi</title>
<link rel=stylesheet href='style.css'>
<body>

	<header>

<% if (loggedIn) { %>

		<p>Logged in as <%= user.id %>. <a href='/logout'>Log out</a>

<% } else { %>

		<form method=post action='/login'>
			<input name=user type=text value=test1>
			<input name=password type=password value='pass1'>
			<input type=submit value='Log in'>
		</form>

<% } %>

	<div class='clear'></div>
	</header>

<% if (messages.length) { %>

	<div class='messages'>
	<% for (var i = 0; i < messages.length; i++) { %>
		<div class='message'><%= messages[i] %></div>
	<% } %>
	</div>

<% } %>

	<div class='content'>
		<h1>Hi there!</h1>
<% if (loggedIn) { %>

		<h2>Hi, <%= user.id %>!</h2>

		<p>Balance: <%= user.balance %>
		<p>Fill up your account balance by sending bitcoins to (TODO insert address).

<% } else { %>

		<h2>The Gimme button is the awesomest way to collect micro-donations.</h2>

		<p>Try it: 

		<div class='gimme-button' data-to='antti@sykari.net'></div>
		<script src='http://gimme.sykari.net/button.js'></script>

<!--		<p>Install it:

		<textarea>some text here
		</textarea> -->

		<p>Create a new account <a href='/new'>here</a> to get started!

<% } %>
	</div>
</body>
